spring:
  application:
    name: config-server
  cloud:
    config:
      discovery:
        enabled: false


management:
  endpoints:
    enabled-by-default: false

eureka:
  client:
    enabled: false
    register-with-eureka: false